#spring.application.name=ConfigService
server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: ConfigService
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
